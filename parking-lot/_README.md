# parking-lot/

Temporary staging for skills that have not yet been placed in a phase/topic.

- Add an entry to `unplaced-skills.yaml` when you have a skill idea but haven't
  decided where it belongs.
- Move it into a proper topic folder once the placement is decided.
- Do not link directly to parking-lot entries from external sources — they are
  not stable.
